name: pre-commit

on:
  pull_request:
    branches: [bixin_dev]

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v2
    - uses: actions/cache@v1
      with:
        path: ~/.cache/pre-commit
        key: pre-commit|${{ env.PY }}|${{ hashFiles('.pre-commit-config.yaml') }}
    - name: Run pre-commit test
      run: |
        git fetch origin bixin_dev
        pip install pre-commit
        pre-commit install
        pre-commit run --show-diff-on-failure --color=always --from-ref origin/$GITHUB_BASE_REF --to-ref $GITHUB_HEAD_REF || tail /home/runner/.cache/pre-commit/pre-commit.log
